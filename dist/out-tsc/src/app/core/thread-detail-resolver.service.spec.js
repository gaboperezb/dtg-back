import { TestBed } from '@angular/core/testing';
import { ThreadDetailResolverService } from './thread-detail-resolver.service';
describe('ThreadDetailResolverService', () => {
    beforeEach(() => TestBed.configureTestingModule({}));
    it('should be created', () => {
        const service = TestBed.get(ThreadDetailResolverService);
        expect(service).toBeTruthy();
    });
});
//# sourceMappingURL=thread-detail-resolver.service.spec.js.map